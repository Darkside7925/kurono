# Kurono OS Apps

Place your application source code here.
Example structure:
- `calculator.cpp`
- `calculator.h`

To include apps in the build, update `build_kurono_cpp.ps1` to include the source files.
